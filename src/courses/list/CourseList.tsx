import React from "react";

import { Panel } from "@erkenningen/ui";

const CourseList: React.FC<{}> = (props) => {
  return <Panel title="Bijeenkomst seelcteren">TODO</Panel>;
};

export default CourseList;
