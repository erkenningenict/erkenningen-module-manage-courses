import React from 'react';

import { Panel } from '@erkenningen/ui/layout/panel';

const CourseReady: React.FC<{}> = (props) => {
  return <Panel title="Nieuwe bijeenkomst maken en plannen">KLAAR</Panel>;
};

export default CourseReady;
